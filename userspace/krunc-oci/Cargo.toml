[package]
name = "krunc-oci"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0"

[dependencies]
krunc-abi = { path = "../krunc-abi" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
